dependencies:
  override:
    - make deps

test:
  override:
    - make test

deployment:
  build:
    branch: /^.+$/
    commands:
      - make build
      - cp -R bun/ecs-formation $CIRCLE_ARTIFACTS/
